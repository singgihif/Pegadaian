@model IEnumerable<WebPegadaian.Models.Installment>

@{
    ViewBag.Title = "Index";
}

<div class="breadcrumbs">
    <div class="col-sm-4">
        <div class="page-header float-left">
            <div class="page-title">
                <h1>Data Installment</h1>
            </div>
        </div>
    </div>
</div>

<div class="content mt-3">
    <div class="animated fadeIn">
        <div class="row">

            <div class="col-md-12">
                <div class="card">
                    <div class="card-header">
                        @Html.ActionLink("Create Installment", "Create")
                    </div>
                    <div class="card-body">
                        <table id="bootstrap-data-table" class="table table-striped table-bordered">
                            <thead>
                                <tr>
                                    <th>No</th>
                                    <th>Installment Date</th>
                                    <th>Due Date</th>
                                    <th>Amount</th>
                                    <th>Installment Amount</th>
                                    <th>Transaction Approve Status</th>
                                    <th>Action</th>
                                </tr>
                            </thead>
                            <tbody>
                                @foreach (var item in Model)
                                {
                                <tr>
                                    <td></td>
                                    <td>@Html.DisplayFor(modelItem => item.Installment_Date)</td>
                                    <td>
                                        @Html.DisplayFor(modelItem => item.Due_Date)
                                    </td>
                                    <td>
                                        @Html.DisplayFor(modelItem => item.Amount)
                                    </td>
                                    <td>
                                        @Html.DisplayFor(modelItem => item.Installment_Amount)
                                    </td>
                                    <td>
                                        @Html.DisplayFor(modelItem => item.Transaction.Approved_Status)
                                    </td>
                                    <td>
                                        <a type="button" class="btn btn-warning" href="@Url.Action("Edit", "Edit", new { id = item.Id })">Edit</a>
                                        <a type="button" class="btn btn-danger" href="@Url.Action("Delete", "Delete", new { id = item.Id })">Danger</a>
                                    </td>
                                </tr>
                                }
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>


        </div>
    </div><!-- .animated -->
</div><!-- .content -->


